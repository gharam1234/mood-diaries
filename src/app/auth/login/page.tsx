import AuthLogin from '@/components/auth-login';

/**
 * 로그인 페이지
 * 
 * AuthLogin 컴포넌트를 사용하여 모던한 로그인 페이지를 렌더링합니다.
 * - 공통컴포넌트 Input, Button 활용
 * - 모던한 디자인 스타일 적용
 * - 반응형 디자인 지원
 */
export default function LoginPage() {
  return <AuthLogin />;
}
